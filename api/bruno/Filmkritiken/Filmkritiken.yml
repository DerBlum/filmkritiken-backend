info:
  name: Filmkritiken
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/filmkritiken?limit=50&offset=0"
  params:
    - name: limit
      value: "50"
      type: query
    - name: offset
      value: "0"
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
